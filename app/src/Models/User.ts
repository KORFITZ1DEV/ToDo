export type UserProfileToken = {
    userName: string;
    email: string;
    token: string;
    userID: string;
}

export type UserProfile = {
    userName: string,
    email: string
    userID: string

}